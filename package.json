{
  "name": "wufly-pwa",
  "version": "1.0.0",
  "description": "Wufly Progressive Web App",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "vercel-build": "echo 'Static Deploy'",
    "dev": "node build.js --watch"
  },
  "dependencies": {
    "esbuild": "^0.25.0"
  }
}
